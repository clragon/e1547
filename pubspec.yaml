name: e1547
description: A mobile app for e621+e926.
version: 1.8.1+42
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:

  # widgets
  expandable: ^5.0.1
  like_button: ^2.0.2
  photo_view: ^0.12.0
  sliver_tools: ^0.2.5
  sliding_sheet: ^0.5.0
  video_player: ^2.2.5
  pull_to_refresh: ^2.0.0
  decorated_icon: ^1.2.0
  cached_network_image: ^3.0.0
  font_awesome_flutter: ^9.0.0
  flutter_typeahead: ^3.2.1
  flutter_cache_manager: ^3.0.1
  infinite_scroll_pagination: ^3.1.0

  flutter_staggered_grid_view:
    git: git://github.com/clragon/flutter_staggered_grid_view.git

  # data
  filesize: ^2.0.1
  timeago: ^3.1.0
  mutex: ^3.0.0
  dio: ^4.0.0
  share: ^2.0.1
  url_launcher: ^6.0.12
  wakelock: ^0.5.6
  intl: ^0.17.0
  permission_handler: ^8.1.6
  shared_preferences: ^2.0.8
  image_gallery_saver: ^1.7.0

  username_generator:
    git: git://github.com/clragon/username_generator.git

  flutter:
    sdk: flutter
  collection: ^1.15.0-nullsafety.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: ^0.9.0
  flutter_native_splash: ^1.1.7

flutter:
  uses-material-design: true
  assets:
    - assets/icon/round.png

# flutter pub run flutter_launcher_icons:main
flutter_icons:
  ios: true
  remove_alpha_ios: true
  android: "launcher_icon"
  adaptive_icon_background: "assets/icon/app/background.png"
  adaptive_icon_foreground: "assets/icon/app/foreground.png"
  image_path: "assets/icon/app/icon.png"

# flutter pub run flutter_native_splash:create
flutter_native_splash:
  color: "000000"
  image: "assets/icon/app/round.png"